<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardBackgroundColor="@color/surface"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="VOLUMES"
            android:textColor="@color/text_secondary"
            android:textSize="10sp"
            android:letterSpacing="0.15"
            android:layout_marginBottom="10dp"/>

        <!-- Vocal volume -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="2dp">
            <TextView android:layout_width="70dp" android:layout_height="wrap_content"
                android:text="🎙 Vocal" android:textColor="@color/text_primary" android:textSize="13sp"/>
            <com.google.android.material.slider.Slider
                android:id="@+id/slider_vocal_vol"
                android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1"
                android:valueFrom="0" android:valueTo="100" android:value="100"
                app:thumbColor="@color/accent_purple"
                app:trackColorActive="@color/accent_purple"
                app:trackColorInactive="@color/surface_variant"/>
        </LinearLayout>

        <!-- Track volume -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">
            <TextView android:layout_width="70dp" android:layout_height="wrap_content"
                android:text="🎵 Track" android:textColor="@color/text_primary" android:textSize="13sp"/>
            <com.google.android.material.slider.Slider
                android:id="@+id/slider_track_vol"
                android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1"
                android:valueFrom="0" android:valueTo="100" android:value="80"
                app:thumbColor="@color/accent_cyan"
                app:trackColorActive="@color/accent_cyan"
                app:trackColorInactive="@color/surface_variant"/>
        </LinearLayout>

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
